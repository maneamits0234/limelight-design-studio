
  # Generate Design from Requirements

  This is a code bundle for Generate Design from Requirements. The original project is available at https://www.figma.com/design/DjKocg6kirjhd2hlESjmHS/Generate-Design-from-Requirements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  